type Author implements Character {
    id: ID!
    name: String!
    age: Int
}

type Book implements Character {
    id: ID!
    name: String!
    author: Author
    publisher: String
}

interface Character {
  id: ID!
  name: String!
}