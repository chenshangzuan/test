server:
  port: 9002

spring:
  boot:
    admin:
      client:
        url: 'http://localhost:10001'
        username: admin
        password: admin

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: 'always'